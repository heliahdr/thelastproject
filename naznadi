numbers = [1, 2, 'three', 4, 5]

total = 0
for num in numbers:
    try:
        if num % 2 == 0:
            total == total + num  # Incorrect assignment operator
    except ValueError:
        print(" Invalid number found")

print(f"Total of even numbers: {total}")
